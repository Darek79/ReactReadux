import React, {Fragment} from 'react';

export const Header = (props,{page, text}) => {
    console.log(props)
    return(
        <Fragment>
            <h2>darek</h2>
            <h2>skwarek</h2>
        </Fragment>
        )
}